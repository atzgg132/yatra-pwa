# Disabled. PNG icons are already in the repo. Auto-decode on push
# failed repeatedly (missing files / bad base64) and emailed on every commit.
name: Write PWA PNG icons
on:
  workflow_dispatch:
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Icons already committed. This workflow does not run on push."
