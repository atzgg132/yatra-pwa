# Disabled. GitHub Pages cannot be enabled on this repo from Actions
# (token cannot create a Pages site). The live PWA is:
# https://atzgg132.github.io/
name: Deploy GitHub Pages
on:
  workflow_dispatch:
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "No-op. Hosted at https://atzgg132.github.io/"
